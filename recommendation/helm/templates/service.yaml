apiVersion: v1
kind: Service
metadata:
  name: recommendation
  labels:
    app: recommendation
spec:
  selector:
    app: recommendation
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
